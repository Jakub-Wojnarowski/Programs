#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include "base.h"

using namespace std;

void equalize(base*);
int str_to_int(string);
int convert(char);
int calculate_damage(int, int, double, vector<base*>*);


#endif